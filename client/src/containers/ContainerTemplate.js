import { connect } from 'react-redux';
import Template from '../components/TemplateComponent'



const mapState = (state, ownProps) => ({

})

const mapDispatch = (dispatch, ownProps) => ({

});

export default connect(mapState, mapDispatch)(Template);
