import { connect } from 'react-redux';
import HomepageShortProse from '../components/HomepageShortProseComponent'



const mapState = (state, ownProps) => ({

})

const mapDispatch = (dispatch, ownProps) => ({

});

export default connect(mapState, mapDispatch)(HomepageShortProse);
