import { connect } from 'react-redux';
import Cause from '../components/CauseComponent'



const mapState = (state, ownProps) => ({

})

const mapDispatch = (dispatch, ownProps) => ({

});

export default connect(mapState, mapDispatch)(Cause);
