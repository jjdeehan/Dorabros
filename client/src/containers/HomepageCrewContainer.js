import { connect } from 'react-redux';
import HomepageCrew from '../components/HomepageCrewComponent'



const mapState = (state, ownProps) => ({

})

const mapDispatch = (dispatch, ownProps) => ({

});

export default connect(mapState, mapDispatch)(HomepageCrew);
