import { connect } from 'react-redux';
import Sponsorship250 from '../components/Sponsorship250Component'



const mapState = (state, ownProps) => ({

})

const mapDispatch = (dispatch, ownProps) => ({

});

export default connect(mapState, mapDispatch)(Sponsorship250);
