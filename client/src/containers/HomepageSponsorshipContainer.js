import { connect } from 'react-redux';
import HomepageSponsorship from '../components/HomepageSponsorshipComponent'



const mapState = (state, ownProps) => ({

})

const mapDispatch = (dispatch, ownProps) => ({

});

export default connect(mapState, mapDispatch)(HomepageSponsorship);
