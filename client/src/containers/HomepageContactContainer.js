import { connect } from 'react-redux';
import HomepageContact from '../components/HomepageContactComponent'



const mapState = (state, ownProps) => ({

})

const mapDispatch = (dispatch, ownProps) => ({

});

export default connect(mapState, mapDispatch)(HomepageContact);
