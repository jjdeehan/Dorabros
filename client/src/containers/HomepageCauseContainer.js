import { connect } from 'react-redux';
import HomepageCause from '../components/HomepageCauseComponent'



const mapState = (state, ownProps) => ({

})

const mapDispatch = (dispatch, ownProps) => ({

});

export default connect(mapState, mapDispatch)(HomepageCause);
