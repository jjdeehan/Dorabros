import { connect } from 'react-redux';
import MarathonDesSables from '../components/MarathonDesSablesComponent'



const mapState = (state, ownProps) => ({

})

const mapDispatch = (dispatch, ownProps) => ({

});

export default connect(mapState, mapDispatch)(MarathonDesSables);
