import { connect } from 'react-redux';
import Crew from '../components/CrewComponent'



const mapState = (state, ownProps) => ({

})

const mapDispatch = (dispatch, ownProps) => ({

});

export default connect(mapState, mapDispatch)(Crew);
